# uk.co.vedaconsulting.extidandmembershiptype

Assign a unique numeric external identifier for a new contact.

## Configuration

Initial ID can be configured via the hidden setting `civi_max_membership_no`.
